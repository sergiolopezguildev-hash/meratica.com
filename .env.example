# Copia este fichero a `.env` para desarrollo local:
#   cp .env.example .env
#
# BASE_URL es la URL pública del sitio (sin barra final).
# build.sh la usa siempre: zola build --base-url "$BASE_URL"

BASE_URL=https://meratica.sergio-lopez-guil-dev.workers.dev

# Opcional: fijar versión de Zola al descargar en CI
# ZOLA_VERSION=0.22.1
